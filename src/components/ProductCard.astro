---
const { name, price, image } = Astro.props;
---
<div class="bg-white p-4 rounded-lg shadow-md hover:shadow-lg transition transform hover:-translate-y-1">
  <img src={image} alt={name} class="w-full h-48 object-cover rounded-md" />
  <h3 class="text-lg font-semibold mt-2">{name}</h3>
  <p class="text-street-accent">${price}</p>
  <button class="mt-2 bg-street-black text-white px-4 py-2 rounded hover:bg-street-accent transition w-full">
    Add to Cart
  </button>
</div>